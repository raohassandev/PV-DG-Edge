# Codex Prompt — Phase 05 Acquisition Worker

Implement driver loader/validator, Modbus TCP polling, RTU config validation, register decoder, poll groups, communication stats, MQTT telemetry publish, and simulated Modbus tests.

Run lint, typecheck, and tests. Report files changed, commands run, test results, known limitations, and next recommended step.
