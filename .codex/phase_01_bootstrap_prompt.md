# Codex Prompt — Phase 01 Bootstrap

Bootstrap pnpm TypeScript monorepo with apps/api, acquisition-worker, aggregation-worker, rules-worker, web and packages/shared, config, logger, telemetry, drivers, db. Add lint, test, typecheck, env example, health endpoint, Docker skeleton. Do not implement polling yet.

Run lint, typecheck, and tests. Report files changed, commands run, test results, known limitations, and next recommended step.
